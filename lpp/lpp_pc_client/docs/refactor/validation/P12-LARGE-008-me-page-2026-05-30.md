# P12-LARGE-008 MePage 瘦身验证

日期：2026-05-30

## 变更

- 新增 `src/renderer/components/MePrivacySections.tsx`。
- 将朋友权限、黑名单查询、隐私设置 mutation 和隐私值映射 helper 从 `MePage.tsx` 拆出。
- `MePage.tsx` 从 736 行降至 541 行。

## 验证命令

```bash
wc -l src/renderer/components/MePage.tsx src/renderer/components/MePrivacySections.tsx
npx tsc --noEmit --pretty false --skipLibCheck
npm run p12:audit
```

## 结果

- 类型检查通过。
- `MePage.tsx` 已低于 P12 组件 700 行观察线。
- `p12:audit` 不再列出 `MePage.tsx`。

## 遗留

- 账号隐私接口行为依赖真实后端数据，后续 Windows/联调环境可补手工验证。
