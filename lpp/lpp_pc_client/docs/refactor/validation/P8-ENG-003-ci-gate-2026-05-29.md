# P8-ENG-003 CI Gate

日期：2026-05-29

## 修改范围

- 新增仓库根目录 `.github/workflows/pc-client-ci.yml`。
- 更新 `playwright.config.ts`，默认使用 Playwright Chromium；如需本机 Chrome，可设置 `PLAYWRIGHT_CHROME_CHANNEL=chrome`。

## CI 门禁

| Job | 命令 |
| --- | --- |
| `quick` | `npm ci`、`npm run check:quick`、`npm run test:core`、`npm run build` |
| `browser` | `npm ci`、`npx playwright install --with-deps chromium`、`npm run test:browser` |

## 验证命令

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `ruby -e "require 'yaml'; YAML.load_file(...)"` | 通过 | workflow YAML 可解析。 |
| `playwright test --list` | 通过 | 识别 5 个 browser tests。 |
| `npm run check:quick` | 通过 | 本地 quick gate 通过。 |

## 注意

- 本任务只添加本地仓库 workflow 文件，不推送、不触发远程 CI。
- Electron 打包 `dist:win` 暂不进 CI，Windows 打包应后续单独设计 runner 和签名策略。
