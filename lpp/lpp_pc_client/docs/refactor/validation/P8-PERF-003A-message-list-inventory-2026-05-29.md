# P8-PERF-003A Message List Inventory

日期：2026-05-29

## 盘点结论

- `MessageListPanel` 原实现对 `messages` 直接 `map`，长会话会一次性创建全部消息 DOM。
- 搜索结果、历史筛选和未读跳转依赖消息元素定位，不能直接粗暴裁剪。
- 当前阶段不引入虚拟列表库，先做可回滚的尾部分段渲染，降低长会话默认 DOM 压力。

## 约束

- 搜索打开时全量渲染，确保结果定位可用。
- 历史筛选打开时全量渲染，确保历史结果可滚动定位。
- 未读跳转存在时全量渲染，避免目标消息被隐藏。
